# Tech Debt Tracker

| ID | Area | Debt | Impact | Next Step |
| --- | --- | --- | --- | --- |
| TD-001 | Repo | DuckDB-backed dev 闭环已覆盖 latest snapshot、as-of、analysis、screener、backtest、source-backed sync、最小历史回补、retry/backoff、alerts、resident scheduler、Tushare canonical provider 的离线 / live 主链、CNInfo 官方披露元数据 sidecar、企业行为 sidecar / reconciliation、canonical quality checks，以及 AKShare/BaoStock 补充校验；当前仍缺更完整的长期补数策略、更深企业行为口径和更稳的 AKShare 链路 | 当前已能稳定开发与验证更大研究池下的最小研究闭环，也能跑真实 canonical sync、财务 sidecar、官方披露 metadata、企业行为 sidecar / reconciliation、字段级 shadow validation、复权/停牌/涨跌停 canonical checks、隔离历史回补、lookback/target-start-date coverage 扩展、建议的 next target start date 暴露、`target_start_biz_date=auto` 调度收敛和基础调度 catch-up；2026-07-03 latest-artifact live smoke 已验证 `2025-11-17 -> 2026-07-03` 的 152 open days，正式 live coverage 至少推进到 `2025-08-26 -> 2026-07-03`，离完整 v1.0 盘后生产运行仍差多年批次补数、旧 raw snapshot refresh 口径、更深企业行为修正，以及 AKShare 上游链路稳定性 | 补可审计 raw snapshot refresh / reingest 口径，清理正式库历史旧 raw 缺少 `adj_factor_overrides` watermark 导致的 `unchanged_adj_factor=9` warning；继续设计多年补数 checkpoint、source quota 预算和失败续跑 |
| TD-002 | Data | 最小 v1.0 DDL 已落地，并新增 `fundamental_feature_daily` 财务 sidecar，但 benchmark 历史、原始财务事实表和更完整任务/请求状态仍有缺口 | 数据层现在可支撑开发 workbench，并开始消费 canonical 财务分，但真实研究与运维约束仍不够全 | 继续按里程碑补齐 benchmark/原始财务/任务状态相关表与口径 |
| TD-003 | Reliability | 任务状态、durable request、阶段化 worker、retry/backoff、alerts、workbench alerts 可视化、alert summary、resident scheduler、JSONL tick、JSONL 落盘/轮转、operations runbook、ops doctor、after-close check、fixture/live pipeline canary、macOS launchd 示例和本机 launchd live 验证已落地；基础 alert hygiene 已区分运维错误、交易触发和历史确认；第一版 webhook 主动通知桥已覆盖 ERROR、provider degradation 和 after-close hard gate failure，但仍缺持久化确认记录和更细粒度 source/provider 失败分类 | 盘后链路已有最小可靠性护栏；2026-04-09 已用 Tushare 在 `core_research_12` 跑通 resident daemon canary，自动 catch-up 到 `2026-04-09` 且 alerts=0；2026-07-03 本机 launchd live runtime 已验证 backend/frontend/pipeline 均 running，pipeline settled，source freshness 与 HTTP/log 门禁通过；`--fail-on-alert` 现在只拦未确认运维 error，盘中交易触发 error 不再误判为运行时故障；启用 webhook 后 runtime alerts 可主动触达外部通知通道并按 fingerprint 节流 | 把 env cutoff 升级为可审计的 alert acknowledgement 记录，并继续细化 source/provider 级失败分层 |
| TD-004 | Quality | 文档校验与 smoke 未接入 CI | 结构可能随时间失效 | 后续补最小 CI job |
| TD-005 | Testing | app-level smoke 与 pipeline smoke 已覆盖 stock/screener/backtest detail、source sync 和 retry 路径，但仍缺单元测试和浏览器级断言 | 页面和流水线结构已有回归保护，但复杂逻辑仍主要靠集成 smoke 兜底 | 后续补 bootstrap/repo 单测和浏览器级 UI smoke |
| TD-006 | Execution Harness | execution harness 已托住多里程碑开发，但 active/completed plan 归档和 CI 自动门禁还没落地 | 长任务虽可连续推进，但历史计划会逐渐累积在 `active/` 下 | 后续补 completed 归档和最小 CI gate，并把 app-level smoke 接进持续门禁 |
| TD-007 | Backfill | 长窗口 live 回补已能靠 target date 推进，`tushare.dividend` 已改成进程内按 symbol 缓存，queue/worker 的 `history_backfill` 已改成 latest artifact 模式，source-only 中间日会跳过重 artifact 输入并保留 `adj_factor_overrides`；当前仍缺真实多年初始化的分批 checkpoint、限流预算和旧 raw refresh 口径 | 自动补数已经能工作，长窗口不再必须按交易日重复发布全量 price series/analysis/screener/backtest；2026-07-03 隔离 live smoke 152 open days 首轮 `190.174s`、rerun `0.712s`、`artifact_snapshot_count=1`，正式 daemon 已把 coverage 推过 `2025-11-18` 到至少 `2025-08-26` | 继续观察 daemon 后续 auto 追深；补多年 backfill 的批次大小、checkpoint、source quota 预算、失败续跑，以及旧 raw snapshots 的 adj_factor refresh 流程 |
