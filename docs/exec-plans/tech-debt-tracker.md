# Tech Debt Tracker

| ID | Area | Debt | Impact | Next Step |
| --- | --- | --- | --- | --- |
| TD-001 | Repo | DuckDB-backed dev 闭环已覆盖 latest snapshot、as-of、analysis、screener、backtest、source-backed sync、最小历史回补、retry/backoff、alerts、resident scheduler、Tushare canonical provider 的离线 / live 主链、CNInfo 官方披露元数据 sidecar、企业行为 sidecar / reconciliation、canonical quality checks，以及 AKShare/BaoStock 补充校验；当前仍缺更完整的长期补数策略、更深企业行为口径和更稳的 AKShare 链路 | 当前已能稳定开发与验证更大研究池下的最小研究闭环，也能跑真实 canonical sync、财务 sidecar、官方披露 metadata、企业行为 sidecar / reconciliation、字段级 shadow validation、复权/停牌/涨跌停 canonical checks、隔离历史回补、lookback/target-start-date coverage 扩展、建议的 next target start date 暴露、`target_start_biz_date=auto` 调度收敛和基础调度 catch-up；而且 live coverage 已推进到 `2025-12-15 -> 2026-04-08` 的 74 个 open days，`corporate_action` reconciliation 达到 `checked=9/aligned=9/boundary_gap=0`，离完整 v1.0 盘后生产运行仍差更长窗口 backfill 运维策略、更深企业行为修正，以及 AKShare 上游链路稳定性 | 先优化长窗口 snapshot/price-series rebuild 的 CPU 与写库成本；优化后再把历史覆盖窗口从当前 `2025-12-15` 往 `2025-11-18` 之前推进，并扩大 `corporate_action` 的 in-coverage 比例 |
| TD-002 | Data | 最小 v1.0 DDL 已落地，并新增 `fundamental_feature_daily` 财务 sidecar，但 benchmark 历史、原始财务事实表和更完整任务/请求状态仍有缺口 | 数据层现在可支撑开发 workbench，并开始消费 canonical 财务分，但真实研究与运维约束仍不够全 | 继续按里程碑补齐 benchmark/原始财务/任务状态相关表与口径 |
| TD-003 | Reliability | 任务状态、durable request、阶段化 worker、retry/backoff、alerts、workbench alerts 可视化、alert summary 和 resident scheduler 已落地，但仍缺远端通知、自动 supervisor、运行手册和更细粒度的失败分类 | 盘后链路已有最小可靠性护栏，降级也已开始对使用者可见，但仍不算真正可运维 | 继续补告警下游、部署侧常驻守护和 source/provider 级故障分层 |
| TD-004 | Quality | 文档校验与 smoke 未接入 CI | 结构可能随时间失效 | 后续补最小 CI job |
| TD-005 | Testing | app-level smoke 与 pipeline smoke 已覆盖 stock/screener/backtest detail、source sync 和 retry 路径，但仍缺单元测试和浏览器级断言 | 页面和流水线结构已有回归保护，但复杂逻辑仍主要靠集成 smoke 兜底 | 后续补 bootstrap/repo 单测和浏览器级 UI smoke |
| TD-006 | Execution Harness | execution harness 已托住多里程碑开发，但 active/completed plan 归档和 CI 自动门禁还没落地 | 长任务虽可连续推进，但历史计划会逐渐累积在 `active/` 下 | 后续补 completed 归档和最小 CI gate，并把 app-level smoke 接进持续门禁 |
| TD-007 | Backfill | 长窗口 live 回补已能靠 target date 推进，且 `tushare.dividend` 已改成进程内按 symbol 缓存；但当前 source-backed sync 仍会对历史窗口逐日重建 snapshot/price series，进入 70+ open days 后，耗时开始明显转向本地 CPU/写库成本 | 自动补数已经能工作，但如果直接推到多年历史会非常慢，不适合作为最终历史初始化路径 | 先把长窗口回补拆成“append daily facts + 增量重建最新 READY price series/sidecars”，或增加批量初始化路径；完成前，不要盲目一次性把企业行为覆盖推到多年以前 |
