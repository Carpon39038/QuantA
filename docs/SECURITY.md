# Security Notes

## Scope

当前项目主要面对本地单机部署，但安全边界仍需明确。

## Rules

1. API key、Cookie、数据库凭证只放环境变量或本地私有配置。
2. 不把真实敏感凭证写入仓库、样例脚本或文档。
3. 外部数据源适配器必须明确来源和用途，避免隐式抓取。
4. Web 查询默认只读，不提供任意写入型管理接口。
5. 后续若接入智能体执行能力，默认采用审批和白名单命令。

## Future Work

1. 配置模板与密钥注入规范
2. 数据源访问频率与封禁风险说明
3. 本地任务执行权限边界
